openapi: '3.0.2'
info:
  title: API Title
  version: '1.0'
  description: 'service for converting video based on ffmpeg'
servers:
  - url: http://localhost[:port]/
paths:
  /api/convert:
    get:
      responses:
        '200':
          description: OK
